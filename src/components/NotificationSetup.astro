---
// No server-side logic needed for this component
---

<div class="bg-white/70 backdrop-blur-sm rounded-2xl p-6 shadow-lg">
  <div id="notification-status">
    <!-- Default state -->
    <div id="notification-default" class="text-center">
      <div class="text-4xl mb-4">🔔</div>
      <h3 class="text-lg font-semibold text-love-700 mb-2">Activar Notificaciones</h3>
      <p class="text-love-600 mb-4 text-sm">
        Para recibir mensajes de amor cada mañana
      </p>
      <button 
        id="enable-notifications" 
        class="w-full bg-love-500 text-white py-3 px-6 rounded-xl font-semibold hover:bg-love-600 transition-colors shadow-lg hover:shadow-xl transform hover:-translate-y-0.5 transition-all duration-200"
      >
        💕 Activar Notificaciones
      </button>
    </div>

    <!-- Loading state -->
    <div id="notification-loading" class="text-center hidden">
      <div class="text-4xl mb-4 animate-pulse">⏳</div>
      <h3 class="text-lg font-semibold text-love-700 mb-2">Configurando...</h3>
      <p class="text-love-600 text-sm">Un momento, por favor</p>
    </div>

    <!-- Success state -->
    <div id="notification-success" class="text-center hidden">
      <div class="text-4xl mb-4">✅</div>
      <h3 class="text-lg font-semibold text-green-700 mb-2">¡Listo! 🎉</h3>
      <p class="text-green-600 text-sm mb-2">
        Notificaciones activadas correctamente
      </p>
      <p class="text-xs text-love-500 bg-love-50 rounded-lg p-2">
        Recibirás un mensaje hermoso cada mañana a las 7:00 AM
      </p>
    </div>

    <!-- Error state -->
    <div id="notification-error" class="text-center hidden">
      <div class="text-4xl mb-4">❌</div>
      <h3 class="text-lg font-semibold text-red-700 mb-2">Oops!</h3>
      <p class="text-red-600 text-sm mb-4" id="error-message">
        No se pudieron activar las notificaciones
      </p>
      <button 
        id="retry-notifications" 
        class="bg-red-500 text-white py-2 px-4 rounded-lg text-sm hover:bg-red-600 transition-colors"
      >
        Intentar de nuevo
      </button>
    </div>

    <!-- Blocked state -->
    <div id="notification-blocked" class="text-center hidden">
      <div class="text-4xl mb-4">🚫</div>
      <h3 class="text-lg font-semibold text-orange-700 mb-2">Notificaciones Bloqueadas</h3>
      <p class="text-orange-600 text-sm mb-4">
        Para recibir mensajes, necesitas habilitar las notificaciones manualmente
      </p>
      <div class="text-xs text-orange-500 bg-orange-50 rounded-lg p-3 text-left space-y-1">
        <p><strong>Chrome:</strong> Icono 🔒 → Notificaciones → Permitir</p>
        <p><strong>Safari:</strong> Configuración → Este sitio web → Notificaciones → Permitir</p>
      </div>
    </div>
  </div>
</div>

<style>
  .animate-pulse {
    animation: pulse 2s cubic-bezier(0.4, 0, 0.6, 1) infinite;
  }
  
  @keyframes pulse {
    0%, 100% {
      opacity: 1;
    }
    50% {
      opacity: .5;
    }
  }
</style>