---
import Layout from '../layouts/Layout.astro';
import NotificationSetup from '../components/NotificationSetup.astro';
import LoveMessage from '../components/LoveMessage.astro';
---

<Layout title="Y&E App 💕">
  <main class="relative container mx-auto h-screen px-4 py-8 max-w-md bg-cover bg-center bg-no-repeat bg-[url('../assets/bg-app.png')]">
    <!-- Overlay para opacidad -->
    <div class="absolute inset-0 bg-white/80"></div>
    
    <!-- Contenido principal -->
    <div class="relative h-full flex flex-col justify-between items-center  z-10">
      <!-- Header -->
      <div class="text-center mb-8">
        <div class="text-6xl mb-4">💕</div>
        <h1 class="text-3xl font-bold text-love-700 mb-2">Y&E App</h1>
        <p class="text-love-600">Mensajes de amor que llegan cada mañana</p>
      </div>

      <!-- Main Content -->
      <div class="space-y-6">
        <!-- Today's Message -->
        <LoveMessage />
        
        <!-- Notification Setup -->
        <NotificationSetup />      
      </div>

      <!-- Footer -->
      <div class="text-center mt-12 text-love-400 text-sm font-extrabold">
        <p>Hecho con 💖 especialmente para ti</p>
      </div>
    </div>
  </main>

  <script src="../scripts/notification-manager.ts"></script>
</Layout>